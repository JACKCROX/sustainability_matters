function myFunction(){
    var element = document.body;
    element.dataset.bsTheme =
    element.dataset.bsTheme == "light" ? "dark" : "light"; //toggle between light and dark mode (if light, change to dark, if dark, change to light)
}

